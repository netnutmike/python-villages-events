#!/usr/bin/env bash
# Wrapper script for villages-events command
# This ensures the correct Python path is set

SCRIPT_DIR="$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")"
export PYTHONPATH="$SCRIPT_DIR:$PYTHONPATH"

exec python -m src.villages_events "$@"
